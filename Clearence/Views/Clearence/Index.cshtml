@model IEnumerable<Clearence.Models.Entities.Clearence>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.RegistrationNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsFinance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeanOfStudent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsRegistrar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClearenceDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.RegistrationNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsFinance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeanOfStudent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsRegistrar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClearenceDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
